
# build a Dataset that loads in a whole dataset
# split into train, val, test
# tokenize input on demand, map to GloVE vector
# keep a vocab class around, vocab class maps words into vectors (called by PyTorch)
# ability to map label into integer